import React from 'react';

// This file is a conceptual placeholder and is architecturally incorrect.
// New user accounts (including staff like receptionists) are created by an Administrator
// via the Staff Management dashboard, not through a public-facing registration form.
// Patient registration is handled via the central `pages/auth/RegisterForm.tsx`.

const ReceptionistRegister: React.FC = () => {
  return (
    <div>
      {/* This is a placeholder. User registration is handled centrally. */}
    </div>
  );
};

export default ReceptionistRegister;
